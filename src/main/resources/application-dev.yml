spring:
  datasource:
    url: jdbc:mysql://localhost:3306/receipt_db_dev
    username: root
    password:
    driverClassName: com.mysql.cj.jdbc.Driver
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
#    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: receipt_database_create.sql
    